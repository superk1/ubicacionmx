name: ubicacionmx_nueva
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.7.0
  geolocator: ^12.0.0
  flutter_dotenv: ^5.1.0
  
  # Usamos http para una llamada de red directa y robusta a la API de Direcciones
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/   # Para el logo
    - .env      # Para la clave de API de Direcciones
